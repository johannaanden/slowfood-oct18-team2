%h1 Today's Menu

%span.shop-icon
  %span#order-details.shop-quantity 1 item
- @categories.each do |category|
  %p= category.category
  %div{id: dom_id(category)}
    - category.products.each do |product|
      %p= product.name
      %p= product.description
      %p= product.price
